export class GameCard {
  constructor(id, title, thumb, desc, genre, platform) {
    this.id = id;
    this.title = title;
    this.thumb = thumb;
    this.desc = desc;
    this.genre = genre;
    this.platform = platform;
  }
}
